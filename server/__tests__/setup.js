
// __tests__/setup.js
jest.setTimeout(11000); // Increase timeout for MongoDB operations

// Suppress console.log during tests
global.console = {
  ...console,
  log: jest.fn(),
  debug: jest.fn(),
  info: jest.fn(),
  warn: jest.fn(),
  error: jest.fn(),
};